# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
'on': pull_request
jobs:
  build_and_preview:
    runs-on: ubuntu-latest
		steps:
    - uses: actions/checkout@master
    - uses: actions/setup-node@master
      with:
        node-version: '12.x'
    - run: npm install
    - run: npm run build
    # Documentation in : https://github.com/w9jds/firebase-action
    - uses: w9jds/firebase-action@master
      env:
        FIREBASE_TOKEN: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_SLINQER_LANDING }}
    # steps:
    #   - uses: actions/checkout@v2
    #   - uses: FirebaseExtended/action-hosting-deploy@v0
    #     with:
    #       repoToken: '${{ secrets.GITHUB_TOKEN }}'
    #       firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_SLINQER_LANDING }}'
    #       projectId: slinqer-landing
    #     env:
    #       FIREBASE_CLI_PREVIEWS: hostingchannels
